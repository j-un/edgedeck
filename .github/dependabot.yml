version: 2
updates:
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
    groups:
      dev-dependencies:
        dependency-type: development
      production-dependencies:
        dependency-type: production
    ignore:
      - dependency-name: 'eslint'
        versions: ['>=10']
      - dependency-name: '@eslint/*'
        versions: ['>=10']
      # typescript-eslint requires typescript <6.1.0; TS 7 has no programmatic API yet
      - dependency-name: 'typescript'
        versions: ['>=7']

  - package-ecosystem: terraform
    directory: /terraform
    schedule:
      interval: weekly

  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
